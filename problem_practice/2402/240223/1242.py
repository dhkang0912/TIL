T = int(input())
for tc in range(1, T+1):
    N, M = map(int, input().split())

    # 16진수 M
    Arr = []

    # 이진수 M*4
    newArr = []

    for row in range(1, N):
        row, col => 위에가 0이고 끝에서 처음 나타나는 1

    # 코드를 찾을 때 비율로 찾아야 함
    #while 1인 경우 갯수 0인 경우 갯수
    # => 8개가 모일 때까지
